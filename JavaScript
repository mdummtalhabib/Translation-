function translateText() {
  const input = document.getElementById("inputText").value;

  // Dummy translation
  const translated = input.split('').reverse().join('');

  document.getElementById("outputText").innerText = translated;
}